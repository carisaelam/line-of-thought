<%= turbo_stream.replace dom_id(@post, :like_buttons) do %>
  <%= render partial: "posts/like_buttons", locals: { post: @post } %>
<% end %>

<%= turbo_stream.replace "like-count" do %>
  <%= render partial: "posts/like_count", locals: { post: @post } %>
<% end %>